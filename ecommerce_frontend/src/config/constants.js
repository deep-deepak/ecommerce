/**
here we declare our backend API constant so we don't have to rewrite it every time
**/

const constants = {
  API_URL: "http://127.0.0.1:8000",
};

/**
to avoid spelling mistakes we use KEYS
**/
export const KEYS = {
  user: "user",
  seller: "seller",
  customer: "customer",
  admin: "admin",
};

export default constants;
